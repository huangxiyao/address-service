<IfModule !mod_proxy_ajp.c>
        LoadModule proxy_ajp_module modules/mod_proxy_ajp.so
</IfModule>

<IfModule !mod_rewrite.c>
        LoadModule rewrite_module modules/mod_rewrite.so
</IfModule>

<Proxy balancer://casfw-security-proxy>
        BalancerMember ajp://d9t0138g.houston.hp.com:25809
</Proxy>

ProxyPass /security balancer://casfw-security-proxy/security
